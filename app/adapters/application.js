import FirebaseAdapter from 'emberfire/adapters/firebase';

// Aquí no hay que hacer absolutamente nada, este es el adapter file que se añade
// automáticamente al instalar ember-fire.

export default FirebaseAdapter.extend({
});
